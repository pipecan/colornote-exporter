[project]
name = "colornote-exporter"
version = "0.1.0"
description = "Decrypt and export ColorNote backup notes."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pycryptodome",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "ruff>=0.15.6",
]
